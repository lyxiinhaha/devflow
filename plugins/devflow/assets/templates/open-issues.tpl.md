# 开放问题：{title}

> **工作项 ID**：{id}
> 本文件是开放问题的持久化托管池。被「先放一放」的问题记录在此，新会话恢复时不会丢失，直到被显式关闭。
> devflow:continue 恢复时优先读取此文件，展示 open/paused 条目。

<!-- 类型枚举：
  blocker     — 阻塞主线，必须解决（对应 HardBlocker）
  assumption  — 显式假设，记录四要素后允许推进（对应 Assumption/ControlledPass）
  integration — 集成阶段发现的跨模块问题
  regression  — 变更后需要回归验证的已完成任务
  optimization — 不阻主线的优化项（对应 OptItem）
-->

<!-- 状态枚举：
  open       — 活跃，需要处理
  paused     — 已知但先搁置（用户说「先放一放」时更新为此状态）
  resolved   — 已解决，恢复触发器已触发
  wont-fix   — 明确不处理，用户确认后设为此状态
-->

## 问题列表

| # | 问题描述 | 类型 | 状态 | 证据/来源 | 影响范围 | 恢复触发器 | 剩余动作 | 关联任务 |
|---|---------|------|------|---------|---------|-----------|---------|---------|
